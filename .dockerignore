# Keep the build context lean. (The Dockerfile copies only the static assets,
# so config files are never served even if present in the context.)
.git
.gitignore
.vscode
.dockerignore
Dockerfile
docker-compose.yml
DEPLOY.md
*.sample.json

# Never bake the live transactions file into the image — it is host-mounted.
lastTrans.json
